using UnityEngine;

public class blablabla : MonoBehaviour {
    public interface IAddRemoveReload {
        public abstract void AddElement(int insertId);


        public abstract void RemoveElement(int RemoveId);

    }
}